#!/bin/sh
# Paludis hook for dynusers' addgroup.bash and adduser.bash scripts

prefix=@prefix@
datarootdir=@datarootdir@
package=@PACKAGE@
package=${package/-paludis}
scriptdir=@datadir@/${package}

"${scriptdir}/add.bash" "${ENEWUSERS}" "${ENEWGROUPS}" "${CATEGORY}/${PF}" \
	"${USERLAND}-${ELIBC}"
